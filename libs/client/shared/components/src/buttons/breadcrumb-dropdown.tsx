import styled from 'styled-components';

export const BreadcrumbDropdown = styled.select`
  border: none;
  background-color: var(--material-blue-grey-50);
  color: var(--material-blue-grey-800);
  font-weight: bold;
  padding: 8px;
  font-size: 16px;
`;

export default BreadcrumbDropdown;
